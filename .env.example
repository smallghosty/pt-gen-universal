# PT-Gen 环境变量配置示例
# 复制此文件为 .env 并填入你的配置（仅用于 Node.js/Bun 运行时）

# API 访问密钥（可选）
# 设置后需在请求中添加 ?apikey={APIKEY}
APIKEY=your-api-key-here

# 禁用搜索功能（可选）
# 设置为任意非空值时禁用搜索功能
DISABLE_SEARCH=false

# 豆瓣 Cookie（可选）
# 用于访问登录可见资源
DOUBAN_COOKIE=your-douban-cookie-here

# indienova Cookie（可选）
# 参考：https://github.com/Rhilip/pt-gen-cfworker/issues/15
INDIENOVA_COOKIE=your-indienova-cookie-here

# TMDB API 密钥（可选）
# 用于访问 TMDB 资源，获取地址：https://www.themoviedb.org/settings/api
TMDB_API_KEY=your-tmdb-api-key-here

# 服务器端口（仅 Node.js/Bun）
# 默认：3000
PORT=3000

# 缓存过期时间（秒）
# 默认：172800（2天）
# 示例：86400（1天）、604800（7天）、0（禁用缓存）
CACHE_TTL=172800
